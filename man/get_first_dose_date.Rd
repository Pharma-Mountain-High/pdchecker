% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_date.R
\name{get_first_dose_date}
\alias{get_first_dose_date}
\title{Get First Dose Date for Each Subject}
\usage{
get_first_dose_date(data, ex_datasets = "EX", ex_date_var = "EXSTDAT")
}
\arguments{
\item{data}{List containing clinical trial datasets}

\item{ex_datasets}{Character vector, exposure dataset names (default: "EX").
Multiple datasets can be specified, e.g., c("EX1", "EX2")}

\item{ex_date_var}{Character vector, dosing start date variable names (default: "EXSTDAT").
\itemize{
\item If length 1, all datasets use the same column name
\item If length equals \code{ex_datasets}, corresponds one-to-one with datasets
}}
}
\value{
Data frame with columns:
\describe{
\item{SUBJID}{Character. Subject ID}
\item{first_dose_date}{Date. First dose date}
}
}
\description{
Extract the earliest dosing start date from exposure datasets for each subject.
}
\details{
\subsection{Calculation Rules}{
\itemize{
\item Extracts \strong{earliest} dosing start date from all specified exposure datasets
\item Always uses dosing \strong{start date} variable (\code{ex_date_var})
\item Supports multiple datasets, automatically merges and takes minimum date per subject
\item SAS missing values (NA, ".", "") are automatically excluded
}
}

\subsection{Multiple Datasets Support}{

When multiple datasets are specified:
\itemize{
\item All datasets are combined
\item For each subject, the minimum date across all datasets is returned
\item Missing datasets are silently skipped
}
}
}
\examples{
\dontrun{
# Basic usage with single dataset
data <- list(
  EX = data.frame(
    SUBJID = c("001", "001", "002"),
    EXSTDAT = c("2024-01-01", "2024-01-29", "2024-01-05"),
    stringsAsFactors = FALSE
  )
)
first_dates <- get_first_dose_date(data)
# Subject 001: 2024-01-01 (min of 2024-01-01 and 2024-01-29)
# Subject 002: 2024-01-05

# Multiple datasets with same column name
data <- list(
  EX1 = data.frame(
    SUBJID = c("001", "001"),
    EXSTDAT = c("2024-01-15", "2024-02-01"),
    stringsAsFactors = FALSE
  ),
  EX2 = data.frame(
    SUBJID = c("001", "002"),
    EXSTDAT = c("2024-01-10", "2024-01-20"),
    stringsAsFactors = FALSE
  )
)
first_dates <- get_first_dose_date(
  data,
  ex_datasets = c("EX1", "EX2"),
  ex_date_var = "EXSTDAT"
)
# Subject 001: 2024-01-10 (min across EX1 and EX2)
# Subject 002: 2024-01-20

# Multiple datasets with different column names
data <- list(
  EX1 = data.frame(
    SUBJID = "001",
    STDAT1 = "2024-01-15",
    stringsAsFactors = FALSE
  ),
  EX2 = data.frame(
    SUBJID = "001",
    STDAT2 = "2024-01-10",
    stringsAsFactors = FALSE
  )
)
first_dates <- get_first_dose_date(
  data,
  ex_datasets = c("EX1", "EX2"),
  ex_date_var = c("STDAT1", "STDAT2")
)
# Subject 001: 2024-01-10
}

}
\seealso{
\code{\link{get_last_dose_date}} for extracting last dose dates

Other date extraction: 
\code{\link{get_eos_date}()},
\code{\link{get_eot_date}()},
\code{\link{get_last_dose_date}()}
}
\concept{date extraction}
