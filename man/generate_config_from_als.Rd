% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_config_from_als.R
\name{generate_config_from_als}
\alias{generate_config_from_als}
\title{Generate Visit and Test Config Files from ALS Using AI}
\usage{
generate_config_from_als(
  als_file,
  visitcode_file = NULL,
  testconfig_file = NULL,
  output_dir = NULL,
  write_files = TRUE,
  overwrite = FALSE,
  exclude_visits = c("共同页", "计划外访视"),
  exclude_forms = NULL,
  ai_instruction = NULL,
  verbose = FALSE
)
}
\arguments{
\item{als_file}{Character. Path to the ALS Excel file (.xlsx).}

\item{visitcode_file}{Character. Output path for visit schedule file.
If \code{NULL} and \code{write_files = TRUE}, writes \code{visitcode.xlsx} under \code{output_dir}.}

\item{testconfig_file}{Character. Output path for test config file.
If \code{NULL} and \code{write_files = TRUE}, writes \code{testconfig.xlsx} under \code{output_dir}.}

\item{output_dir}{Character. Directory for output files when paths are \code{NULL}.
Defaults to the directory containing \code{als_file}.}

\item{write_files}{Logical. If \code{TRUE}, write Excel files to disk (default).}

\item{overwrite}{Logical. If \code{FALSE} (default), stop when output files already exist.}

\item{exclude_visits}{Character vector. Visit names to exclude from visitcode
(default: \code{c("共同页", "计划外访视")}).}

\item{exclude_forms}{Character vector. Form labels to exclude from testconfig.}

\item{ai_instruction}{Character. Optional hints for AI (sheet/column names, exclusions).}

\item{verbose}{Logical. Print raw AI response.}
}
\value{
An invisible list with elements:
\describe{
\item{visitcode}{Data frame for \code{read_visitcode_file}}
\item{testconfig}{Data frame for \code{read_testconfig_file}}
\item{visitcode_file}{Path written, or \code{NULL}}
\item{testconfig_file}{Path written, or \code{NULL}}
\item{ai_notes}{Character, notes from AI on sheet/column mapping}
}
}
\description{
Read an ALS (eCRF) Excel workbook, use an LLM to identify visit schedule and
visit-matrix structure across projects, and generate the two external
configuration files required by pdchecker.
}
\details{
The function serializes all worksheet content (truncated per sheet) and sends
it to an OpenAI-compatible API. The model returns JSON with \code{visitcode} and
\code{testconfig} tables compatible with \code{\link{read_visitcode_file}} and
\code{\link{read_testconfig_file}}.

Set \code{OPENROUTER_API_KEY}, \code{OPENROUTER_BASE_URL}, and \code{OPENROUTER_MODEL} in
\code{.Renviron}. Use \code{ai_instruction} to describe project-specific sheet names or
column meanings when layouts differ.
\subsection{What AI Identifies (priority)}{

\strong{visitcode} — from the visit schedule sheet (访视 / Visit / Folders):
\itemize{
\item \code{VISIT}: visit name
\item \code{VISITNUM}: visit code/number (must match EDC, not matrix column order)
\item \code{VISITDAY}: inferred from \code{VISIT} when absent in ALS (e.g. D-28, D1, EOT)
\item \code{CYCLE}: inferred from \code{VISIT} — 预激、预治疗、筛选期、治疗期1/治疗期2…、
治疗结束、研究结束、随访期; unrecognized visits left empty
\item \code{WP}: left \strong{empty} for manual completion
}

\strong{testconfig} — from the eCRF visit matrix (检查类 only):
\itemize{
\item \code{TESTCAT}: Chinese only, strip parenthetical codes (血常规, not 血常规(LB) or LB)
\item Excludes visit admin, ICF, demographics, medical history, AE, concomitant meds, etc.
\item \code{VISITNUM}: comma-separated visit numbers for required checks
\item \code{FORM} kept for reference when editing
}
}
}
\examples{
\dontrun{
cfg <- generate_config_from_als(
  als_file = system.file("extdata", "ALS_VISIT.xlsx", package = "pdchecker"),
  output_dir = tempdir(),
  ai_instruction = "访视编号在访视 sheet 的 VISITNUM 列"
)
visitcode <- read_visitcode_file(cfg$visitcode_file)
testconfig <- read_testconfig_file(cfg$testconfig_file, visitcode = visitcode)
}

}
