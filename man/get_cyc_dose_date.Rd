% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_date.R
\name{get_cyc_dose_date}
\alias{get_cyc_dose_date}
\title{Get Dosing Date per Subject and Visit}
\usage{
get_cyc_dose_date(
  data,
  ex_datasets = getOption("pdchecker.ex_datasets", "EX"),
  ex_date_var = getOption("pdchecker.ex_date_var", "EXSTDAT"),
  ex_time_var = getOption("pdchecker.ex_time_var", NULL),
  visitnum_var = getOption("pdchecker.sv_visitnum_var", "VISITNUM")
)
}
\arguments{
\item{data}{List containing clinical trial datasets}

\item{ex_datasets}{Character vector, exposure dataset names (default: "EX").
Multiple datasets can be specified, e.g., c("EX1", "EX2")}

\item{ex_date_var}{Character vector, dosing start date variable names (default: "EXSTDAT").
\itemize{
\item If length 1, all datasets use the same column name
\item If length equals \code{ex_datasets}, corresponds one-to-one with datasets
}}

\item{ex_time_var}{Character vector, dosing start time variable names (default: NULL).
If NULL or empty, \code{cyc_dose_time} is returned as NA.
\itemize{
\item If length 1, all datasets use the same column name
\item If length equals \code{ex_datasets}, corresponds one-to-one with datasets
}}

\item{visitnum_var}{Character string, visit number variable name in the
exposure datasets (default: "VISITNUM")}
}
\value{
Data frame with columns:
\describe{
\item{SUBJID}{Character. Subject ID}
\item{VISITNUM}{Character. Visit number}
\item{cyc_dose_date}{Date. Dosing date at this visit}
\item{cyc_dose_time}{Character. Dosing time at this visit (NA if unavailable)}
}
}
\description{
Extract the dosing date (and optional dosing time) at each visit from
exposure datasets for each subject. Used as the EX anchor via
\code{\link{prepare_test_data}} and \code{\link{generate_test_window_dates}}.
}
\details{
\subsection{Calculation Rules}{
\itemize{
\item Extracts the \strong{earliest} dosing start date per subject per visit
(multiple dosing records within one visit take the minimum date)
\item When \code{ex_time_var} is specified, also returns the dosing time from the
earliest-date record (among same-date records, the earliest non-missing
time string is preferred)
\item Supports multiple datasets, automatically merges before taking the minimum
\item Missing datasets are silently skipped
\item SAS missing values (NA, ".", "") are automatically excluded
\item VISITNUM is returned as character for joining with prepared test data
}
}
}
\examples{
\dontrun{
data <- list(
  EX = data.frame(
    SUBJID = c("001", "001", "001", "002"),
    VISITNUM = c(2, 2, 3, 2),
    EXSTDAT = c("2024-01-01", "2024-01-02", "2024-01-29", "2024-01-05"),
    EXSTTIM = c("09:00", "08:00", "10:00", "14:00"),
    stringsAsFactors = FALSE
  )
)
cyc_dose_dates <- get_cyc_dose_date(data, ex_time_var = "EXSTTIM")
# Subject 001 / VISITNUM 2: 2024-01-01 09:00 (min of two records)
# Subject 001 / VISITNUM 3: 2024-01-29 10:00
# Subject 002 / VISITNUM 2: 2024-01-05 14:00
}

}
\seealso{
\code{\link{get_first_dose_date}} for extracting first dose dates
\code{\link{get_rand_date}} for extracting randomization dates
\code{\link{prepare_test_data}} for attaching cyc_dose_date to prepared test data

Other date extraction:
\code{\link[=get_eos_date]{get_eos_date()}},
\code{\link[=get_eot_date]{get_eot_date()}},
\code{\link[=get_first_dose_date]{get_first_dose_date()}},
\code{\link[=get_last_dose_date]{get_last_dose_date()}},
\code{\link[=get_rand_date]{get_rand_date()}}
}
\concept{date extraction}
