% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_date.R
\name{get_rand_date}
\alias{get_rand_date}
\title{Get Randomization Date for Each Subject}
\usage{
get_rand_date(
  data,
  rd_datasets = getOption("pdchecker.rd_datasets", "RAND"),
  rd_date_var = getOption("pdchecker.rd_date_var", "RANDDT")
)
}
\arguments{
\item{data}{List containing clinical trial datasets}

\item{rd_datasets}{Character vector, randomization dataset names (default: "RAND").
Multiple datasets can be specified, e.g., c("RAND", "DSRAND")}

\item{rd_date_var}{Character vector, randomization date variable names (default: "RANDDT").
\itemize{
\item If length 1, all datasets use the same column name
\item If length equals \code{rd_datasets}, corresponds one-to-one with datasets
}}
}
\value{
Data frame with columns:
\describe{
\item{SUBJID}{Character. Subject ID}
\item{rd_date}{Date. Randomization date}
}
}
\description{
Extract the randomization date from RAND datasets for each subject.
Used as the RD anchor date via \code{\link{prepare_test_data}} and
\code{\link{generate_test_window_dates}}.
}
\details{
\subsection{Calculation Rules}{
\itemize{
\item Extracts the \strong{earliest} randomization date per subject
\item Supports multiple datasets, automatically merges before taking the minimum
\item Missing datasets are silently skipped
\item SAS missing values (NA, ".", "") are automatically excluded
}
}
}
\examples{
\dontrun{
data <- list(
  RAND = data.frame(
    SUBJID = c("001", "002"),
    RANDDT = c("2024-01-15", "2024-01-20"),
    stringsAsFactors = FALSE
  )
)
rd_dates <- get_rand_date(data)
}

}
\seealso{
\code{\link{get_cyc_dose_date}} for extracting per-visit dosing dates
\code{\link{prepare_test_data}} for attaching rd_date to prepared test data

Other date extraction:
\code{\link[=get_cyc_dose_date]{get_cyc_dose_date()}},
\code{\link[=get_eos_date]{get_eos_date()}},
\code{\link[=get_eot_date]{get_eot_date()}},
\code{\link[=get_first_dose_date]{get_first_dose_date()}},
\code{\link[=get_last_dose_date]{get_last_dose_date()}}
}
\concept{date extraction}
