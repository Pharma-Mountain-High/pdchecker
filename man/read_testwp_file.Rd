% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_testwp_file.R
\name{read_testwp_file}
\alias{read_testwp_file}
\title{Read Test Window Configuration File}
\usage{
read_testwp_file(file_path, sheet_name = "Sheet1", visitcode = NULL)
}
\arguments{
\item{file_path}{Character. File path (\code{.xlsx} or \code{.xls}).}

\item{sheet_name}{Character. Excel sheet name (default: \code{"Sheet1"}).}

\item{visitcode}{Data frame from \code{\link{read_visitcode_file}} (default: \code{NULL}).
If provided (or found as \code{visitcode} in the calling environment), visit names
from visitcode are joined by \code{VISITNUM} for validation.}
}
\value{
A data frame with columns:
\describe{
\item{TESTCAT}{Test category (character)}
\item{VISITNUM}{Visit number (character)}
\item{VISIT}{Visit name from the file (character)}
\item{wp_rule}{Original cell value, e.g. \code{EX(≤24h)}}
    \item{ref}{Reference date type: \code{RD}, \code{SV}, \code{EX}, or \code{FD}}
\item{wp}{Window period string inside parentheses}
\item{type}{Parsed window type (\code{±}, \code{≤}, \code{0}, etc.)}
\item{wpvalue}{Parsed window value (numeric; may be \code{NA}).
Unit is days when \code{wp_unit} is \code{"d"}, hours when \code{"h"}}
\item{wp_unit}{\code{"h"} if the rule uses hours (h/H/小时), otherwise \code{"d"}}
}
}
\description{
Read a test-window matrix Excel file and expand it into one row per
required test at each visit, with parsed window rules.
}
\details{
\subsection{File layout}{

The input file uses a \strong{matrix} layout (unlike \code{\link{read_testconfig_file}}):
\itemize{
\item Row 1: headers — column 1 \code{VISIT}, column 2 \code{VISITNUM},
remaining columns are \code{TESTCAT} names.
\item Following rows: one visit per row; non-empty cells indicate that the test
is required at that visit and contain the window rule.
}
}

\subsection{Window rule format}{

Each non-empty cell uses \code{REF(WP)}:\tabular{ll}{
   REF \tab Anchor date \cr
   RD \tab Randomization date \cr
   SV \tab Actual visit date at this visit \cr
   EX \tab Actual dosing date at this visit \cr
   FD \tab First dose date \cr
}


\code{WP} uses the same syntax as the \code{WP} column in
\code{\link{read_visitcode_file}} (\code{±3d}, \code{≤24h}, \code{+3d}, etc.).
\code{0} inside parentheses means the test must be performed on the anchor day.

Examples: \code{RD(-7d)}, \code{RD(0)}, \code{EX(≤24h)}, \code{FD(±3d)}, \code{SV(±3d)}.
}
}
\examples{
\dontrun{
testwp <- read_testwp_file(
  system.file("extdata", "example_test_wp.xlsx", package = "pdchecker"),
  sheet_name = "QL0911-302"
)
}

}
